import 'package:flutter/material.dart';

void main() => runApp(const MyNewApp());

class MyNewApp extends StatelessWidget {
  const MyNewApp({super.key, required this.des});

  final des;
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
